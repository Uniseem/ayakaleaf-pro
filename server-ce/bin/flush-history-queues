#!/bin/bash

set -euo pipefail

source /etc/container_environment.sh

# Writes every project's queued history into the store, and waits for it.
# What the nightly sweep does, on demand.
exec curl --fail --silent --show-error -X POST \
  'http://127.0.0.1:3054/flush/old?timeout=0&limit=100000'
