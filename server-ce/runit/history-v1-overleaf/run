#!/bin/bash

# The history store (services-go/cmd/history): every version of every file.

source /etc/overleaf/env.sh
export LISTEN_ADDRESS=127.0.0.1

exec /sbin/setuser www-data /overleaf/bin/go/history >> /var/log/overleaf/history-v1.log 2>&1
