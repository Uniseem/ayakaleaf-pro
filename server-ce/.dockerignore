.DS_Store
.git/
services/git-bridge
server-ce/server-ce
server-ce/test

# The client has its own image, built from services/frontend with its own
# Dockerfile. Nothing in this one reads it.
services/frontend

# The Node web service. Its pages are the client above and its internal API is
# services-go/cmd/api, so nothing in this image runs it -- and it was most of
# the image.
services/web
